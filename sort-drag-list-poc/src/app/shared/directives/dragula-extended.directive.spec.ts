import { DragulaExtendedDirective } from './dragula-extended.directive';

describe('DragulaExtendedDirective', () => {
  it('should create an instance', () => {
    // const directive = new DragulaExtendedDirective();
    // expect(directive).toBeTruthy();
  });
});
